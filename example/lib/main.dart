import 'package:flutter/material.dart';

myfunc(bool istr, String name) {}
void main() {
  runApp(
    MaterialApp(
      home: const Scaffold(
        body: Center(child: const Text('Hello world Galib')),
      ),
    ),
  );
}

// Scafold



// Everything is Widget